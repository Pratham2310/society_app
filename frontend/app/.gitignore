# @generated expo-cli sync-2b81b286409207a5da26e14c78851eb30d8ccbdb
expo-env.d.ts
# @end expo-cli

node_modules/
.expo/
dist/
web-build/

# Native projects are generated by prebuild; the app.json config is the
# source of truth, so a checked-in copy only ever goes stale.
/ios
/android

*.log
npm-debug.*
yarn-debug.*
yarn-error.*

# EXPO_PUBLIC_ vars are compiled into the bundle and are not secret, but
# this one is a machine-specific LAN address that would break everyone
# else's checkout.
.env
.env.local
